apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rtdenoise-dataset-filtered-prebatched
spec:
  storageClassName: rook-cephfs
  accessModes:
    - ReadWriteOncePod
  resources:
    requests:
      storage: 10Ti
